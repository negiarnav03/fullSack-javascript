// array = ["", "", "", ""] // array with strings
// array = [{}, {}, {}] // array with objects

// for of 

const arr = [1,2,3,4,5]
for (const num of arr) {
    console.log(num); // returns values one by one
}

