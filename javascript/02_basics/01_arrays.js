// arrays
/*
const myArr = [1,2,3,4,5];
const myHeros = ["shaktiman", "naagraj", "doga"];
const arr2 = new Array(1,2,3,4,5);

// array methods 

myArr.push(6); // manuplates original array from end
myArr.pop(); // manuplates original array from end
console.log(myArr);

myArr.unshift(9); // manuplates original array from start
myArr.shift(); // manuplates original array from start

console.log(myArr.includes(9));
console.log(myArr.indexOf(9));

const newArr = myArr.join(); // it binds the array into string.

console.log(myArr);
console.log(typeof newArr);
*/

// slice and splice 

/*

const myArr = [1,2,3,4,5];
console.log("A", myArr);

const myn1 = myArr.slice(1,3); 
// it does not manuplate original array and returns the slice copy. it include the start index and exclude the end index.
console.log("B", myArr);
console.log(myn1);

const myn2 = myArr.splice(1,3); 
// it does manuplate original array and returns the removed elements. it include the start index and the number of elements to remove starting from the start position..
console.log("C", myArr);
console.log(myn2);


*/