import React from "react";

const userContext = React.createContext();

// it is for providing context to the children components. 
// or we can say that whatever components we wrap in the userContext.Provider will get the context
// whatever data/values is given to the userContext.Provider will be available to the children components.
 





export default userContext;
