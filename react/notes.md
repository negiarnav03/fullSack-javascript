# to start react
```bash
npx create-react-app notes 

# here notes is the name of the folder which will be created in the current directory. 
# By running this command it will set up the entire environment for the react.

cd notes

npm run start
```

```bash
npm create vite@latest
npm i # to install the latest dependencies.
npm run dev


# to install tailwaind
npm install @tailwindcss/vite


# to call react router dom
npm install react-router-dom


# to install redux toolkit (RTK)
npm install @reduxjs/toolkit react-redux


```


// it must be noted that every react app uses the ReactDOM to render the app.js file.
// every function's name must be capitalized. if not it will throw an error.
// in html file i.e. index.html the <div id="root"></div> is the container where the react app will be rendered.
